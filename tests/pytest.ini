[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    system: loads the integration against a real Clash-compatible core
    release: slower fault, recovery, and lifecycle validation required before release
